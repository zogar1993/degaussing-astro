---
//TODO check for better way to handle fonts, since this is duplicative
// local fonts load faster, but chrome does not chache them for some reason, Mozilla does
// <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=GermaniaOne|Inter&display=fallback">
// did not work

//TODO check if better font-display: fallback;
---

<style is:inline>
    @font-face {
        font-family: "Germania One";
        src: local("Germania One"), local("GermaniaOne"), url("/fonts/GermaniaOne.woff2") format("woff2");
        font-weight: normal;
        font-style: normal;
    }

    @font-face {
        font-family: "Inter";
        src: local("Inter"), url("/fonts/Inter.woff2") format("woff2");
        font-weight: normal;
        font-style: normal;
    }
</style>
