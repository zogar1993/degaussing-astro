---
//TODO check for better way to handle fonts, since this is duplicative
// local fonts load faster, but chrome does not chache them for some reason, Mozilla does
---
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=GermaniaOne">
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inter">
<style is:inline>
    @font-face {
      font-family: 'Germania One';
      src: url('/fonts/GermaniaOne.woff2') format('woff2');
      font-weight: normal;
      font-style: normal;
      font-display: fallback;
    }

    @font-face {
      font-family: 'Inter';
      src: url('/fonts/Inter.woff2') format('woff2');
      font-weight: normal;
      font-style: normal;
      font-display: fallback;
    }
</style>