---
import type { PageInfo } from "@server/GetPageInfo"
const { info } = Astro.props as { info?: PageInfo }
---
{info && <link rel="preload" fetchpriority="high" as="image" href={info.image.optimized} type="image/webp">}