<main>
	<slot />
</main>
<style>
    main {
        display: flex;
        flex-direction: column;
        max-width: var(--strip_width);
        gap: 1rem;

        width: var(--content--desktop_width);
        background-color: var(--decoration_background-color);

        border-radius: var(--border-radius);

        padding: 1rem;
    }

    @media (max-width: 72em) {
        main {
            width: 100%;
            padding: 0;
            background-color: transparent;
        }
    }
</style>
