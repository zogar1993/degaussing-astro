---
const { src } = Astro.props as { src: string }

const innerHTML = src.startsWith("https:") ?
	await (await fetch(`${src}`)).text() :
	(await import(`${src}?raw`)).default
//TODO needs alt text
---
<Fragment set:html={innerHTML} />